import PropTypes from 'prop-types';

const CategoryList = props => {
    return (
        <div>
            <h2 className="text-6xl">Job Category List</h2>
        </div>  
    );
};

CategoryList.propTypes = {
    
};

export default CategoryList;