import PropTypes from 'prop-types';

const Banner = props => {
    return (
        <div>
            <h2>Home Page Banner</h2>            
        </div>
    );
};

Banner.propTypes = {
    
};

export default Banner;